name: swarm-agent
version: 0.2.0
description: "User-controlled swarm plugin — adds /swarm slash command for parallel LLM research (up to 300 workers). Agent cannot call it on its own."
author: "Hermes Agent"
kind: standalone
provides_commands:
  - swarm
